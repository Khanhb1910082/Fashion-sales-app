import 'package:flutter/material.dart';

import '../../models/order.dart';

class BuildOrders extends StatelessWidget {
  const BuildOrders({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('');
  }
}
